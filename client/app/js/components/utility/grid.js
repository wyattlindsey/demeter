const ReactGrid = require('react-bootstrap-grid')

const Grid = {
  Container:    ReactGrid.Container,
  Row:          ReactGrid.Row,
  Column:       ReactGrid.Column,
  Block:        ReactGrid.Block
}

export default Grid