'use strict';

jest.dontMock('../../../app/js/components/widgets/time-location-selectors.react');

var React = require('../../../node_modules/react');
var ReactDOM = require('../../../node_modules/react-dom');
var TestUtils = require('../../../node_modules/react-addons-test-utils');

var TimeLocationSelectors = require('../../../app/js/components/widgets/time-location-selectors.react').default;

var Toolbar = require('../../../app/js/components/toolbar.react').default;
var TestComponent = require('../../../app/js/components/test-component.react').default;

describe('basic rendering', function () {

  it('does not fuck up', function () {
    var testComponent = TestUtils.renderIntoDocument(
    /*istanbul ignore next*/React.createElement(TestComponent, /*istanbul ignore next*/null));


    var testComponentNode = TestUtils.findRenderedDOMComponentWithTag(testComponent, 'h1');
    console.log(testComponent);

    expect(testComponent.textContent).toEqual('Hi There');});


  // it ('draws sliders on the page', () => {
  //   let MySliders = TestUtils.renderIntoDocument(
  //     <TimeLocationSelectors time={{hour: 12, minute: 0}}
  //                             date={{year: 2016, month: 5, date: 21}}
  //                             latitude={23.5}
  //                             longitude={-122}
  //                             />
  //   )
  // })
});